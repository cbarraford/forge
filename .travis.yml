language: go
sudo: false
go: stable

go_import_path: github.com/cbarraford/forge

before_install:
  - touch .env
  - go get

script:
  - make lint
  - make test-cover

after_success:
  - bash <(curl -s https://codecov.io/bash)
